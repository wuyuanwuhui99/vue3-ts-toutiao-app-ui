import stateInterface from './stateInterface'

export const token = (state: stateInterface) => state.token;
export const userData = (state: stateInterface) => state.userData;
export const favoriteChannels = (state: stateInterface) => state.favoriteChannels;
